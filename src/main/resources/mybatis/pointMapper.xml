<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.batch.test.mapper.PointMapper">

    <insert id="insertPoint">

        insert into point(userId, point, pointInfo) values(#{userId}, #{point}, #{pointInfo})

    </insert>


</mapper>